#include "vector3.h"
#include <iostream>
#include <fstream>
#include <chrono>

using namespace std::chrono; // for clocking every second


int main(void) {
	/* TODO:
	 * 1. read input somehow, not sure yet
	 * 2. setup simulation
	 * 3. run simulation
	 * 4. spit out results
	 */
};
